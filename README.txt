COMO O ALGORITMO FUNCIONA:

PEÇO DESCULPAS PELO MICROFONE, o audio ficou bem ruim

1- Deve ser instaladas certas bibliotecas para a execução do codigo, todas podem ser instaladas utilizando os comandos corretos no promt de comando (CMD)
	comandos: pip install numpy
		  pip install matplotlib
		  
2-O algoritmo escolhido foi o BUG1, com as posições predefinas basta se iniciar o programa executando o arquivo no VSCODE.

3-O algoritmo criara uma linha do ponto inicial que tentara atingir o objetivo, ao encontrar um obstaculo o algoritmo troca de metodo e passa a circular 
o objeto, apos uma volta completa ele volta ao ponto do objeto mais proximo do objetivo final e continua seu trajeto. 

4-O algoritmo termina quando o objetivo é atingido, podendo fechar a figura manualmente.